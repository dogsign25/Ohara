server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # API 프록시 (CORS 우회)
    location /api/ {
        proxy_pass http://ohara-api:8080/api/;
        proxy_set_header Host $host;
    }

    # SPA 라우팅
    location / {
        try_files $uri $uri/ /index.html;
    }
}
